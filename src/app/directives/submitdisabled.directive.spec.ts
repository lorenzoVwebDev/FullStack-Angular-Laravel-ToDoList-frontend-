import { SubmitdisabledDirective } from './submitdisabled.directive';

describe('SubmitdisabledDirective', () => {
  it('should create an instance', () => {
    const directive = new SubmitdisabledDirective();
    expect(directive).toBeTruthy();
  });
});
